# 安全边界说明

## 1) 服务端与前端边界

- `VOLC_ACCESS_KEY` / `VOLC_SECRET_KEY` 仅存在服务端环境变量。
- 前端不会接触签名逻辑，也不会看到密钥。

## 2) 访问控制

- 应用使用单一访问密码 `APP_PASSWORD`。
- 登录成功后，通过 session cookie 维持短期登录态。

## 3) 输入安全

- 后端对白名单字段进行严格校验：
  - `prompt` 字符串长度限制
  - `ratio` 白名单
  - `resolution` 白名单

## 4) 滥用防护

- 全局基础限流
- 登录限流
- 生成接口限流

## 5) 日志与敏感信息

- 不打印密钥与密码
- 错误返回尽量使用友好中文提示，不暴露内部细节

## 6) v1 非目标

- 不做用户系统
- 不做数据库持久化
- 不做历史图片存储
